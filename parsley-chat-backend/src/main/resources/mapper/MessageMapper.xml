<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.parsley.parsley_chat.mapper.MessageMapper">

    <insert id="saveMessage" parameterType="com.parsley.parsley_chat.model.mybatisEntity.Message">
        INSERT INTO messages (
            room_id,
            sender_id,
            sender_name,
            content,
            time_stamp
        )
        VALUES (
            #{roomId},
            #{senderId},
            #{senderName},
            #{content},
            #{timestamp}
        )
    </insert>

    <select id="getChatHistoryByRoomId" resultType="com.parsley.parsley_chat.model.mybatisEntity.Message">
        SELECT *
        FROM messages
        WHERE room_id = #{roomId}
    </select>

</mapper>